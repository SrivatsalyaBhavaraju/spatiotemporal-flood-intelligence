# Core
python-dotenv
numpy
pandas
scikit-learn
matplotlib
pillow
requests

# Geospatial (P1/P2)
osmnx
geopandas
shapely
networkx
rasterio
gdal
imdlib

# Ad-hoc source-document extraction (P2 — e.g. georeferencing figures out of PDF reports)
pymupdf
opencv-python-headless

# Graph ML (P3) — do NOT plain `pip install` these, see src/models/gnn/README.md:
#   pip install torch --index-url https://download.pytorch.org/whl/cu126   (CUDA build, not the CPU-only default)
#   pip install torch-geometric
#   pip install torch-geometric-temporal --no-deps   (its torch-scatter/torch-sparse deps have no prebuilt
#                                                       wheel for this torch/Python combo; see README shim)
torch
torch-geometric
torch-geometric-temporal

# NLP (P4)
transformers
sentencepiece
spacy
rapidfuzz
beautifulsoup4
lxml

# Earth Engine / satellite (P2)
earthengine-api

# Optimization (Objective 3, optional)
ortools
pulp

# Dashboard / viz
streamlit
folium
fastapi
uvicorn

# Dev
pytest
jupyter

# Only needed if OSM/Overpass calls fail with SSLCertVerificationError (e.g. behind a
# corporate TLS-inspection proxy like Sophos) -- makes Python defer to the OS cert
# store instead of certifi's bundle. See src/nlp/README.md.
pip-system-certs
